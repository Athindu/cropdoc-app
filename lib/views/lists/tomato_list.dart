import 'package:flutter/material.dart';


class TomatoList extends StatelessWidget {
  const TomatoList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context)  => Scaffold(
    appBar: AppBar(
      title: Text('TomatoList'),
    ),
    body: Center(
      child: Text('TomatoList'),
    ),
  );
}
