import 'package:flutter/material.dart';


class PotatoList extends StatelessWidget {
  const PotatoList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context)  => Scaffold(
    appBar: AppBar(
      title: Text('PotatoList'),
    ),
    body: Center(
      child: Text('PotatoList'),
    ),
  );
}
