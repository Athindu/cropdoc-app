import 'package:flutter/material.dart';


class GrapeList extends StatelessWidget {
  const GrapeList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context)  => Scaffold(
    appBar: AppBar(
      title: Text('GrapeList'),
    ),
    body: Center(
      child: Text('GrapeList'),
    ),
  );
}
