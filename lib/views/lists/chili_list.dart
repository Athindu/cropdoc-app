import 'package:flutter/material.dart';


class ChiliList extends StatelessWidget {
  const ChiliList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context)  => Scaffold(
    appBar: AppBar(
      title: Text('ChiliList'),
    ),
    body: Center(
      child: Text('ChiliList'),
    ),
  );
}
