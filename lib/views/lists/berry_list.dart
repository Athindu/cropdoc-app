import 'package:flutter/material.dart';


class BerryList extends StatelessWidget {
  const BerryList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context)  => Scaffold(
    appBar: AppBar(
      title: Text('BerryList'),
    ),
    body: Center(
      child: Text('BerryList'),
    ),
  );
}
