import 'package:flutter/material.dart';


class CitrusList extends StatelessWidget {
  const CitrusList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context)  => Scaffold(
    appBar: AppBar(
      title: Text('CitursList'),
    ),
    body: Center(
      child: Text('CitursList'),
    ),
  );
}
