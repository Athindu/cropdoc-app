import 'package:flutter/material.dart';


class PestList extends StatelessWidget {
  const PestList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context)  => Scaffold(
    appBar: AppBar(
      title: Text('PestList'),
    ),
    body: Center(
      child: Text('PestList'),
    ),
  );
}
