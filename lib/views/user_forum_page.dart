import 'package:flutter/material.dart';

class UserForum extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      //body: StreamBuilder()

    );
  }
}
